<style>
  .inline-media {
    margin: 2.5rem auto;
    width: 100%;
    max-width: 100%;
  }

  .inline-media img,
  .inline-media video {
    width: 100%;
    height: auto;
    border-radius: var(--style-border-radius-lg, 8px);
    box-shadow: 0 2px 8px rgba(0, 0, 0, 0.08);
    display: block;
  }

  .inline-media audio {
    width: 100%;
    margin-top: 0.5rem;
  }

  /* Embed container for responsive iframes */
  .inline-media__embed-container {
    position: relative;
    width: 100%;
    overflow: hidden;
    border-radius: var(--style-border-radius-lg, 8px);
    box-shadow: 0 2px 8px rgba(0, 0, 0, 0.08);
  }

  .inline-media__embed-container iframe {
    position: absolute;
    top: 0;
    left: 0;
    width: 100%;
    height: 100%;
    border: none;
  }

  /* Default 16:9 aspect ratio for embeds */
  .inline-media--embed .inline-media__embed-container {
    aspect-ratio: 16 / 9;
  }

  /* Vertical aspect ratio for Instagram Reels, TikTok, YouTube Shorts */
  .inline-media--instagram .inline-media__embed-container,
  .inline-media--tiktok .inline-media__embed-container {
    aspect-ratio: 9 / 16;
    max-width: 400px;
    margin: 0 auto;
  }

  /* YouTube Shorts - vertical */
  .inline-media--youtube.inline-media--shorts .inline-media__embed-container {
    aspect-ratio: 9 / 16;
    max-width: 400px;
    margin: 0 auto;
  }

  /* Captions */
  .inline-media figcaption {
    font-family: var(--font-body--family, -apple-system, BlinkMacSystemFont, 'Segoe UI', Roboto, sans-serif);
    font-size: 0.8125rem;
    font-weight: 500;
    color: rgb(var(--color-foreground-rgb, 47,79,79) / 1.00);
    margin-top: 0.875rem;
    text-align: left;
    font-style: italic;
    line-height: 1.5;
    padding: 0.5rem 1rem;
    letter-spacing: 0.01em;
    background: rgb(var(--color-foreground-rgb, 0 0 0) / 0.00);
    border-radius: 6px;
    max-width: 90%;
    margin-left: auto;
    margin-right: auto;
  }

  /* Responsive adjustments */
  @media screen and (max-width: 749px) {
    .inline-media {
      margin: 1.5rem auto;
    }

    /* Make vertical videos take more screen space on mobile */
    .inline-media--instagram .inline-media__embed-container,
    .inline-media--tiktok .inline-media__embed-container {
      max-width: 100%;
    }

    .inline-media figcaption {
      font-size: 0.75rem;
      padding: 0.4rem 0.75rem;
      margin-top: 0.625rem;
    }
  }

  /* Dark mode support (if your theme has it) */
  @media (prefers-color-scheme: dark) {
    .inline-media figcaption {
      color: rgba(255, 255, 255, 0.7);
    }
  }

  /* Loading state for iframes */
  .inline-media__embed-container {
    background: rgb(var(--color-foreground-rgb, 0 0 0) / 0.05);
  }

  /* Instagram specific adjustments */
  .inline-media--instagram iframe {
    border-radius: var(--style-border-radius-lg, 8px);
  }
</style>